package notifications

import (
	"github.com/gin-gonic/gin"
	"gorm.io/gorm"
)

func GetNotifications(context *gin.Context, db *gorm.DB) {
	//uid, _ := context.Get("uid")
	//userId := uid.(uuid.UUID)

}
